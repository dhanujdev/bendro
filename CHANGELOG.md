# Changelog

All notable changes to Bendro are documented here.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Versioning follows [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Added
- **Phase 15 — Observability + CI e2e + Stripe checkout happy-path:** Operational surface area for a Vercel preview + prod deploy. `@sentry/nextjs@10.49.0` installed; three no-op-when-DSN-unset config files at repo root (`sentry.server.config.ts`, `sentry.edge.config.ts`, `sentry.client.config.ts`) + `instrumentation.ts` that loads server/edge config based on `process.env.NEXT_RUNTIME` and re-exports `captureRequestError as onRequestError` so Next's built-in error pipe surfaces server exceptions. `next.config.ts` now wraps the config with `withSentryConfig(...)` unconditionally — inert when `SENTRY_AUTH_TOKEN` is unset (no source-map upload), so wrapping is safe to keep live by default. PostHog wired via a physical client/server split: `src/lib/analytics.ts` refactored to a client-only module that lazy-inits `posthog-js` when `NEXT_PUBLIC_POSTHOG_KEY` is set and falls back to the existing `window.__bendroEvents` buffer otherwise (`trackEvent` signature unchanged — all call sites keep working); `src/lib/analytics-server.ts` is a new `import "server-only"` module that lazy-loads `posthog-node` for RSC / API-route callers. The split keeps `posthog-node`'s `node:fs` dependency physically out of the client bundle (Turbopack chunking failure otherwise during `pnpm build`). GitHub Actions CI at `.github/workflows/ci.yml` with three jobs — `lint-typecheck-test` blocks `build` + `e2e`. E2e job caches Playwright browsers keyed on `pnpm-lock.yaml`, runs `pnpm exec playwright install --with-deps chromium` on cache miss (deps-only on hit), executes `pnpm e2e` with `CI=true` (retries + github reporter), and uploads `test-results/**` + `playwright-report/**` as artifacts on failure. Concurrency group cancels in-progress runs on the same branch. `docs/DEPLOY.md` lists every env var Bendro reads per Preview / Prod (`DATABASE_URL`, `AUTH_SECRET` + `AUTH_URL`, `AUTH_GOOGLE_*` + `AUTH_RESEND_KEY` + `AUTH_EMAIL_FROM`, five Stripe vars, six observability vars, `NEXT_PUBLIC_APP_URL`, `OPENAI_API_KEY`, plus the `E2E_AUTH_BYPASS=never-in-prod` contract) with a Vercel deploy checklist and the rollback protocol pointing at `DB_TOGGLE.md`. `src/config/env.ts` + `.env.example` synced: full `AUTH_*` block, five Sentry vars, two PostHog vars, `STRIPE_PREMIUM_ANNUAL_PRICE_ID` — all optional so missing observability values no-op across environments. Stripe checkout happy-path e2e in `tests/e2e/billing.spec.ts` (2 specs) via offline `page.route()` intercepts: `**/api/billing/checkout` returns a mock `{ sessionId, url }` and a second intercept on the mock Stripe URL returns a stub HTML page — test asserts CTA renders enabled (`data-has-price-id="true"`, `data-signed-in="true"`), click navigates to the mock URL, and the captured POST body is the expected `priceId`; a second spec asserts the `pricing-checkout-error` banner renders the server's `VALIDATION_ERROR` message on a 400 response. `playwright.config.ts` webServer.env now sets `STRIPE_PREMIUM_PRICE_ID=price_e2e_test_premium` so the CTA renders enabled without real Stripe keys; the previous "disabled when unset" e2e in `marketing.spec.ts` has been rewritten to assert the enabled signed-out path (click routes to `/signin?callbackUrl=/pricing`) — disabled-fallback UX stays as a unit-level concern in `tests/unit/`. New runtime deps: `@sentry/nextjs@10.49.0`, `posthog-js@1.369.3`, `posthog-node@5.29.2`. No schema changes. 307 unit tests across 26 files + 18 Playwright specs across 3 files; typecheck clean; `pnpm build` clean (Sentry wrap did not regress the Phase 14 Drizzle-adapter fix). **Deferred:** Vercel project link + preview deploy (requires interactive owner login), player e2e (`/player/demo`) fixture with `getUserMedia` + MediaPipe stubs, onboarding e2e step bindings, server-side PostHog call-site wiring, production monitoring alerts (PagerDuty / Slack), load + performance testing — all filed as Phase 16 scope.
- **Phase 14 — E2E Coverage / Playwright step bindings:** End-to-end harness around the marketing funnel, the signed-in shell, and the paywall-decoration flow accumulated over Phases 9–13. `@playwright/test` installed; chromium-only `playwright.config.ts` at repo root runs `pnpm dev` via `webServer` with `E2E_AUTH_BYPASS=1` and a seeded `AUTH_SECRET`. New scripts `e2e`, `e2e:ui`, `e2e:install`. `vitest.config.ts` excludes `tests/e2e/**` so the vitest runner no longer trips on Playwright's `test.describe()` runtime. Two production-safe bypass seams added: (1) `maybeE2eSession()` in `src/lib/auth.ts` wraps the NextAuth-exported `auth()` and returns a synthetic `Session` built from `e2e_user_id` / `e2e_user_email` / `e2e_user_name` cookies — gated by `NODE_ENV !== "production"` AND `E2E_AUTH_BYPASS === "1"` (physically disabled in prod); (2) parallel `maybeE2eSubscriptionStatus()` on `getSubscriptionStatus()` in `src/services/billing.ts` reads `e2e_subscription_status` (free | active | trialing | past_due | canceled) so premium state flips via cookie without provisioning user rows. **Auth.js Drizzle-adapter blocker fixed** (was deferred to Phase 15): `src/lib/auth.ts` now conditionally attaches `DrizzleAdapter(db, …)` only when `hasDatabaseUrl()` returns true, falling back to `session: { strategy: "jwt" }` otherwise. The `session()` callback reads `token.sub` when `user?.id` is absent so the JWT path resolves a domain userId. Unblocks both `pnpm dev` without `DATABASE_URL` and `pnpm build` page-data collection. Playwright fixture at `tests/e2e/fixtures.ts` exports `test`/`expect`/`E2E_USERS` (free, premium, pastDue with deterministic UUIDs) and `authAs.signInAs(user)` / `authAs.signOut()` helpers that set/clear the four `e2e_*` cookies. E2E specs: `marketing.spec.ts` (7 specs — landing hero, pricing CTA labels, premium CTA disabled when `STRIPE_PREMIUM_PRICE_ID` unset, legal links, FAQ toggle, signed-in redirect, CTA label flip) and `app.spec.ts` (9 specs — /home dashboard + upgrade CTA, /library premium-decorate lock, locked-row click → /account?upgrade=1, /account status badge for free vs active, premium /home hides upgrade, premium /library unlocks, signed-out guardrails). `src/lib/mock-data.ts` gains `hip-opener-deep` (isPremium: true, level: deep, UUID `22222222-2222-4000-8000-000000000007`) so the paywall-decoration flow is exercisable against the mock adapter. No schema changes. No new runtime deps (`@playwright/test` is devDependency only). 307 unit tests across 26 files + 16 Playwright specs across 2 files; typecheck clean; `pnpm build` clean.
- Creator OS framework port: `.claude/` agents, skills, hooks, rules, commands adapted to bendro stack (Next.js 16 / Drizzle / NextAuth / Stripe).
- `.claude/rules/HEALTH_RULES.md` — absolute prohibitions, mandatory disclaimers, pain-feedback flow, pre-existing-condition gating, camera/pose privacy.
- Foundational docs: `docs/AGENT_MEMORY.md`, `docs/BLOCKERS.md`, `docs/DECISIONS.md`, `docs/EXECUTION_LOG.md`, `docs/PHASES.md`, this `CHANGELOG.md`.
- `.claude/checkpoints/ACTIVE.md` + `docs/SESSION_HANDOFF.md` workflow for cross-session continuity.
- **Phase 1 — Test Coverage Baseline:** `@vitest/coverage-v8` reporter wired up with per-service threshold (≥85% lines for `src/services/**`) and global threshold (≥70% lines). `test:coverage` script added. Unit tests added for `billing`, `routines`, `sessions`, `personalization`, and `src/lib/data.ts` adapter (83 tests total, services at 95% line coverage).
- **Phase 2 — API Contract & Validation:** `src/lib/http.ts` with standardized `{ error: { code, message, details? } }` envelope and 8 error codes (VALIDATION_ERROR, INVALID_JSON, NOT_FOUND, UNAUTHENTICATED, FORBIDDEN, CONFLICT, RATE_LIMITED, INTERNAL). All 6 API routes migrated. `docs/specs/openapi/v1/bendro.yaml` Error schema rewritten. Integration tests added for every route (`tests/integration/api/*`) and Gherkin scaffolds in `tests/features/api/*`. 117 tests passing.
- **Phase 3 — Auth (NextAuth):** Auth.js v5 (`next-auth@5.0.0-beta.31`) + `@auth/drizzle-adapter@1.11.2` with database session strategy. `src/lib/auth.ts` is the sole `next-auth` importer (parallel to the Stripe-only-in-`services/billing.ts` rule) and exports `{ handlers, auth, signIn, signOut }`. New Drizzle tables: `accounts`, `auth_sessions` (renamed from NextAuth's default `session` to avoid workout-session collision), `verification_tokens`; `users` extended with `name`/`emailVerified`/`image`. Bootstrap migration `src/db/migrations/0000_loose_ghost_rider.sql` generated (apply deferred to Phase 5). `src/app/api/auth/[...nextauth]/route.ts` re-exports `handlers`. `src/app/signin/page.tsx` server component with Google OAuth + Resend magic-link providers (dynamic based on env) and an "Auth not configured" fallback. `AuthStatus` component in `src/components/auth-buttons.tsx`. `(app)` layout converted to an async server component that redirects unauthenticated users to `/signin?callbackUrl=/home`. ADR-0004 authored; `DECISIONS.md` D-005/D-006. 120 tests passing.
- **Phase 4 — Player Stability:** Camera client (`src/app/player/camera/_components/camera-pose-client.tsx`) now distinguishes `unsupported` (detected on mount when `navigator.mediaDevices.getUserMedia` is missing), `no_camera` (`NotFoundError` / `OverconstrainedError`), `denied` (`NotAllowedError` / `SecurityError`), and generic `error` states — each with its own overlay and recovery guidance. Pose detector throttled to ~30 Hz via `MIN_DETECT_INTERVAL_MS` so high-refresh displays don't starve the UI thread (RAF loop continues to drive canvas + avatar rendering). Pose math is unit-tested: `tests/unit/pose/angles.test.ts` (17 tests) covers `angleAtJoint` (2D/3D, edges) and `isReliable`. `src/lib/pose/angles.ts` is now included in coverage; `vrm-driver.ts` and `landmarks.ts` remain excluded (browser-only deps / constants). BDD scaffold for the camera flow at `tests/features/player/camera.feature`. 137 tests passing.
- **Phase 5 — DB Toggle Hardening:** Explicit `DataAdapter` interface (built via `typeof` on each exported function) + `dataAdapter` object in `src/lib/data.ts`; adding a new operation now forces the function and the interface to move together. Fallback classifier extracted to `src/lib/data-fallback.ts` with 11 dedicated unit tests covering happy paths (`DATABASE_URL is not set`, `ENOTFOUND`, `ECONNREFUSED`, `ETIMEDOUT`, `fetch failed`, `getaddrinfo`, `connect `), rejection of real errors (Zod, unique-constraint, non-Error throws), case-insensitivity, and 80-char truncation. `hasDatabaseUrl()` helper added to `src/db/index.ts` with 3 unit tests. `docker-compose.db.yml` provisions `postgres:16-alpine` on `localhost:5432` (user/pass/db `bendro/bendro/bendro`, volume `bendro-pgdata`, `pg_isready` healthcheck); `pnpm db:local:up` / `down` / `reset` scripts added. `docs/DB_TOGGLE.md` runbook covers mock ↔ local ↔ Neon preview ↔ Neon prod workflow, migration review checklist, and fallback semantics. DECISIONS D-007 (local compose for parity with Neon) and D-008 (DataAdapter contract via `typeof`). 154 tests passing.
- **Phase 8 — Sessions & Streaks Loop:** Completion semantics hardened in `PATCH /api/sessions/[id]` — completed sessions are now immutable, any further PATCH (re-complete, painFeedback update, duration update) returns `409 CONFLICT`. Prevents client bugs from double-triggering streak logic or overwriting final completion metadata. `ERROR_CODES.CONFLICT` (already in the error taxonomy) now wired into the route. Streak rollover is timezone-aware end-to-end: PATCH route pulls `getUserProfile(userId).timezone` only when completing the session (no wasted round-trip on mid-session updates), forwards it to `updateSession(id, patch, { timezone })`, which threads into `updateStreak(userId, timezone)`. `UpdateSessionOptions { timezone?: string }` contract added to the data adapter (backward-compatible; mock branch ignores timezone since there are no streaks in the in-memory mock). `/home` page rewritten as an async RSC: calls `auth()` + redirects unauthenticated users to `/signin?callbackUrl=/home`, pulls real numbers via `getProgress({ userId, days: 30 })`, renders three stat cards (current streak, week minutes, total sessions) plus a longest-streak hint when `longestStreak > 0 && longestStreak !== currentStreak`. `data-testid` on every card + CTA (`home-page`, `home-start-stretching`, `home-stat-streak`, `home-stat-week-minutes`, `home-stat-total-sessions`, `home-longest-streak`, `home-recommended-{slug}`) for Phase-14 Playwright. 4 new unit tests in `tests/unit/streaks.test.ts` assert `previousDate` is DST-safe on US spring-forward (2024-03-10) and fall-back (2024-11-03), and `formatDateInTimezone` correctly resolves `America/New_York` during spring-forward morning + `Asia/Tokyo` across UTC-midnight. 3 new integration tests in `tests/integration/api/sessions.test.ts`: CONFLICT on already-completed PATCH, timezone forwarded to `updateSession` on completion, profile NOT fetched on non-completion PATCHes. BDD scaffolds: `tests/features/sessions/sessions-loop.feature` (10 scenarios) + `tests/features/home/home.feature` (7 scenarios); step bindings deferred to Phase 14. OpenAPI PATCH `/sessions/{id}` now documents `409 CONFLICT` with new `Conflict` response component. 207 tests passing; coverage thresholds green.
- **Phase 7 — Library / Search / Filters:** `ListRoutinesFilter` expanded with five new fields — `q` (free-text, case-insensitive across title/slug/description), `bodyAreas[]` / `avoidBodyAreas[]` (keep / drop rows whose goal maps to any listed area via `GOAL_BODY_AREAS`), `durationBucket` (`short` ≤ 300s, `medium` ≤ 900s, `long` > 900s), and `safetyFlag` (drops `level === "deep"` — conservative proxy, replaced by caution-tag filtering in Phase 11). `applyRoutineFilters` applies every new predicate against both the DB path and the mock fallback so both backends behave identically under test. `GOAL_BODY_AREAS` now exported from `src/services/personalization.ts` so `src/lib/data.ts` shares the single mapping source (no duplicated body-area table). `GET /api/routines` accepts the 5 new query params; the handler normalizes singular `bodyArea` / `avoidBodyArea` strings into the adapter's plural arrays (public URL surface stays simple, internal filter stays multi-valued — ready for Phase 11 multi-select chips). Library page rewritten as an async RSC: reads `searchParams` via `SearchParamsSchema.safeParse` so a malformed URL can't crash the render, calls `auth()` + `getUserProfile(userId)`, and projects the persisted `avoidAreas` + `safetyFlag` through the query server-side (user never re-clicks safety preferences). Subtitle reflects when the safety flag is on. Renders list with `data-testid="library-routine-{slug}"` or empty state. `LibraryFilterBar` client component with URL-state round-trip via `useTransition()` + `router.replace(href, { scroll: false })` — search input with Enter-submit + clear-X, 7 goal chips (emoji + label), 3 level chips, 3 duration-bucket chips (≤5 / 5–15 / >15 min), `N of N routines` count, "Clear filters" link that removes every filter param in one go. `data-testid` on every control for Phase-14 Playwright. OpenAPI `listRoutines` parameters updated with descriptions, enums, and length constraints. BDD scaffolds: `tests/features/api/routines.feature` +8 scenarios (query expansion + error envelope), `tests/features/library/library.feature` (10 scenarios: initial render, URL-state round-trip, clear-search / clear-filters, profile-driven filters auto-applied, safetyFlag banner, empty state, intersection). 8 new unit tests in `tests/unit/data-adapter.test.ts` + 8 new integration tests in `tests/integration/api/routines.test.ts`. 200 tests across 17 files passing; global (≥70%) and service-layer (≥85% lines + functions) coverage thresholds both pass (services at 95.93% lines).
- **Phase 6 — Onboarding & Personalization:** `users` table extended with `safety_flag BOOLEAN NOT NULL DEFAULT false` and `onboarded_at TIMESTAMP` (migration `0001_loud_patriot.sql`). Per `HEALTH_RULES.md §Pre-Existing Condition Gating`, the 4 yes/no pre-existing-condition answers are never persisted — PATCH `/api/me` derives `safetyFlag = any(conditions)` and discards the raw object server-side. `src/services/profile.ts` (Drizzle-backed `getProfile` / `updateProfile` with partial-patch semantics + 9 unit tests). `src/lib/data.ts` + `DataAdapter` extended with `getUserProfile` / `updateUserProfile` (mock fallback via `getMockProfile` / `updateMockProfile`). `GET /api/me` + `PATCH /api/me` with a strict (`.strict()`) Zod schema that rejects client-supplied `userId` and unknown fields; 12 integration tests covering auth, validation, persistence, privacy, and the trust-boundary invariant. `src/services/personalization.ts` gains `filterRoutineCatalog(routines, profile)` — pure function that filters by goals, avoid-area overlap with `GOAL_BODY_AREAS`, and drops `level === "deep"` when `safetyFlag` is true (Phase-11 will replace this with caution-tag filtering); 9 unit tests cover purity, intersection, and unknown-goal graceful handling. Multi-step onboarding UI (`src/app/onboarding/page.tsx`): `intro → goals → focus → avoid → conditions` with amber safety-gate warning and `data-testid` attributes for Phase-14 Playwright. Behind `ONBOARDING_V1_ENABLED` feature flag (`NEXT_PUBLIC_FF_ONBOARDING_V1`, defaults true) with `LegacyOnboarding` fallback. OpenAPI `/me` paths + `UserProfile` / `PreExistingConditions` / `UpdateProfile` (additionalProperties: false) schemas. BDD scaffolds in `tests/features/api/me.feature` (13 scenarios) and `tests/features/onboarding/onboarding.feature`. 184 tests passing.
- **Phase 13 — Marketing Site & Pricing:** Replaced the always-redirects-to-/home root page with a real public marketing surface. `src/app/page.tsx` deleted; `src/app/(marketing)/page.tsx` is now the canonical `/` handler, auth-aware (signed-in → `redirect("/home")`, signed-out → landing page inside the marketing layout). `/pricing` refactored into an RSC (`src/app/(marketing)/pricing/page.tsx`) that reads `auth()` + `getBillingPlans().premium_monthly.priceId` server-side and wires a new `<StartCheckoutButton />` client component (`src/app/(marketing)/pricing/_components/start-checkout-button.tsx`) to `POST /api/billing/checkout`. Three-state button: signed-out → `/signin?callbackUrl=/pricing`, signed-in + configured priceId → POST → redirect `window.location.href` to the Stripe hosted URL, no priceId → disabled with an inline "not configured on this environment" note; every press emits `trackEvent("upgrade.clicked", { source: "pricing", signedIn, hasPriceId })`; checkout errors surface inline (amber, `role="alert"`). Free-plan CTA toggles between "Get started free" → `/signin?callbackUrl=/home` and "Back to app" → `/home`. 7-question FAQ section appended (Free vs Premium, cancelation, refund, data privacy, health-disclaimer, platform availability) using native `<details>` so it works without JavaScript; `data-testid="pricing-faq-item"` for Phase 14 Playwright. New legal pages: `src/app/(marketing)/legal/terms/page.tsx` (8 sections — acceptance, what-Bendro-is + medical disclaimer, accounts with 13/16 age floor, subscriptions with 14-day refund, acceptable use, liability, change notice, contact) and `src/app/(marketing)/legal/privacy/page.tsx` (8 sections — what we collect, on-device pose processing callout matching `SECURITY_RULES.md`, pre-existing-condition no-persist invariant, auth, payment processing via Stripe, GDPR/CCPA rights, analytics PII invariant, contact). Both carry `metadata: Metadata` and `data-testid` hooks. SEO plumbing: new `src/app/sitemap.ts` (Next.js `MetadataRoute.Sitemap` enumerating `/ /pricing /signin /legal/terms /legal/privacy` with per-route priority + changeFrequency, `NEXT_PUBLIC_APP_URL`-based with trailing-slash stripping + `https://bendro.app` fallback) and `src/app/robots.ts` (allows `/ /pricing /signin /legal/`, disallows `/api/ /home /library /player /account /settings /onboarding /medical-guidance`, advertises `{base}/sitemap.xml`). Marketing layout (`src/app/(marketing)/layout.tsx`) rewritten as an async RSC — header CTA label toggles between "Get started" and "Open app" based on session (`data-testid="marketing-cta"`, `data-signed-in` attribute); footer rebuilt from a single-line copyright into a 4-column grid (brand tagline / Product / Legal / Company mailto `hello@bendro.app`), with the HEALTH_RULES-mandated "Not medical advice" closing line. 7 unit tests in `tests/unit/seo-meta.test.ts` (sitemap path completeness, trailing-slash stripping, priority hierarchy, fallback host; robots disallow completeness, allow contents, sitemap URL). BDD scaffolds: `tests/features/marketing/landing.feature` (4 scenarios), `tests/features/marketing/pricing.feature` (7 scenarios covering checkout happy path, failure, disabled state, FAQ), `tests/features/marketing/legal.feature` (3 scenarios); step bindings deferred to Phase 14. No schema changes. No new external deps. No new feature flags. 307 tests across 26 files passing (+7); typecheck clean after clearing stale `.next` cache.
- **Phase 12 — Monetisation Polish / Paywall UX:** Flipped premium gating from Phase 9's "hide entirely" to "decorate + show upsell". `src/app/(app)/library/page.tsx` now fetches `viewerIsPremium = isPremium(userId)` and decorates each row (`data-locked`, `data-premium`, `Sparkles` "Premium" badge, `Lock` affordance); locked routines route to `/account?upgrade=1&routine={slug}` instead of `/player/{slug}`. New `/account` RSC at `src/app/(app)/account/page.tsx` — auth-gated (`redirect("/signin?callbackUrl=/account")`), reads `getSubscriptionStatus(userId)`, renders a plan-status card (`data-status` + colour-coded badge: free / active / trialing / past_due / canceled), and toggles between `<OpenPortalButton />` for premium users and an `Upgrade to Premium` link for free users. Upgrade prompt banner (`data-testid="account-upgrade-banner"`, `role="status"`) shown when `?upgrade=1 && !isPremium`. `OpenPortalButton` (`src/app/(app)/account/_components/open-portal-button.tsx`) is a client component using `useTransition`, POSTs to `/api/billing/portal`, redirects `window.location.href` on success, renders inline amber error on failure; emits `trackEvent("portal.opened", { source: "account" })` on click. New `POST /api/billing/portal` route (`src/app/api/billing/portal/route.ts`) — auth-gated, Zod-validates `{ returnUrl?: string.url() }`, defaults `returnUrl` to `${NEXT_PUBLIC_APP_URL}/account`; error translation: `NO_CUSTOMER` → 409 `CONFLICT`, `STRIPE_SECRET_KEY is not set` → 503 `INTERNAL`. `createCustomerPortalSession(input)` added to `src/services/billing.ts` — throws `NO_CUSTOMER` if the user row has no `stripeCustomerId`; otherwise `stripe.billingPortal.sessions.create({ customer, return_url })`. /home upgrade CTA added above the recommended rail for free users only — full-width gradient Sparkles tile linking to `/account?upgrade=1&source=home` (`data-testid="home-upgrade-cta"`); suppressed entirely for premium users. New telemetry seam `src/lib/analytics.ts` with an `EventName` union (`upgrade.clicked`, `upgrade.completed`, `portal.opened`, `premium.viewed`, `premium.locked_clicked`) — server-branch logs to `console.info`, client-branch pushes to `window.__bendroEvents` so Playwright can assert on it directly without mocks. 4 unit tests in `tests/unit/billing.test.ts` (happy path, NO_CUSTOMER for no-customer-id, NO_CUSTOMER for no-user-row, 503 for unconfigured Stripe). 6 integration tests in `tests/integration/api/billing-portal.test.ts` (401 / 400 malformed / 409 NO_CUSTOMER / 503 / 200 happy / default returnUrl). 4 unit tests in `tests/unit/analytics.test.ts` (server-branch console.info, client-branch queue push, default props, append behaviour). OpenAPI: `POST /billing/portal` added (200 / 400 / 401 / 409 / 503) in `docs/specs/openapi/v1/bendro.yaml`. BDD scaffolds: `tests/features/billing/portal.feature` (6), `tests/features/billing/account.feature` (8), library.feature +3 paywall decoration scenarios, home.feature +2 CTA scenarios; step bindings deferred to Phase 14. No schema changes (Phase 9 added `users.stripeCustomerId` + `stripe_webhook_events`). No new external deps. No new feature flags. 300 tests across 25 files passing; typecheck clean.
- **Phase 11 — Health Safety & Disclaimers:** Single source of truth for every user-facing health disclaimer in `src/lib/disclaimers.ts` (8 surfaces: `onboardingIntro`, `routineStart`, `painPromptLow`, `painPromptMedium`, `painPromptHigh`, `safetyGate`, `aiGeneratedCard`, `marketingPainRelief`). Copy matches `.claude/rules/HEALTH_RULES.md §Mandatory Disclosures` verbatim. Severity field (`neutral` / `caution` / `warn`) drives the visual variant of `<DisclaimerBanner />` (`src/components/disclaimer-banner.tsx`). `painPromptForRating(0–10) → Disclaimer` shares thresholds with `src/services/safety.ts::classifyPainRating` (≥ 7 high, ≥ 4 medium). Pain-feedback module (`src/services/safety.ts`) exports `PAIN_MEDIUM_THRESHOLD = 4`, `PAIN_HIGH_THRESHOLD = 7`, `classifyPainRating(r): "low" | "medium" | "high"` (defensive on non-finite / negative input), and `sessionHadHighPain(painFeedback): boolean` for the recommender. `<DisclaimerBanner />` rendered on the onboarding intro step (`onboardingIntro`), the pre-existing-condition safety-gate step (`safetyGate`), and the player's ready screen (`routineStart`) — replaces all previously-inlined health copy. New `/medical-guidance` page (`src/app/medical-guidance/page.tsx`) renders `painPromptHigh` and lists the clinician categories to seek out, so the `painPromptHigh` CTA `{ label: "Find medical guidance", href: "/medical-guidance" }` resolves to a real route instead of 404. **Pre-existing-condition enforcement tightened:** `filterRoutineCatalog` in `src/services/personalization.ts` now drops any routine where `level !== "gentle"` when `safetyFlag` is true (Phase 6 dropped only `level === "deep"`) — matches the safety-gate copy promise "We'll default your library to gentle routines" literally. **Pain-history deprioritisation:** `routinesWithHighPainHistory(sessions): Set<string>` pure helper + optional `sessionHistory` parameter on `suggestRoutinesForUser(userId, goals, focusAreas, sessionHistory?)`. Routines the user previously reported high pain (≥ 7) on sort to the back of the result list (soft penalty; still reachable in the library, matching HEALTH_RULES §Pain Feedback Flow wording). 12 unit tests in `tests/unit/disclaimers.test.ts` (registry coverage, CTA presence, `painPromptForRating` tiers, surface-enum matches type union), 9 in `tests/unit/safety.test.ts` (classifier tiers + defensive behaviour), 2 updated + 5 new in `tests/unit/personalization.test.ts` (safetyFlag tightening to `level === "gentle"`, pain-history deprioritisation, `routinesWithHighPainHistory` purity). No schema changes. 286 tests across 23 files passing; typecheck clean. Grep audit confirms inline health copy no longer lives outside `disclaimers.ts` (sole exceptions: `disclaimer-banner.tsx` JSDoc quote, `mock-data.ts` stretch-level caution seed, `medical-guidance/page.tsx` dedicated-page prose).
- **Phase 10 — Player polish:** Keyboard-driven player, per-stretch completion burst, mobile-safe layout, camera-tracking pill, and VRM velocity-clamp smoothing. Pure DOM-free helper `src/lib/player/keyboard.ts` (`decideKeyAction({ key, phase, currentIndex, hasModifier, isTyping })` → tagged union `"pause-toggle" | "start" | "next" | "previous" | "exit" | "none"`) lets Vitest (node env) cover the full keyboard matrix without jsdom; the React component is a thin dispatcher. Global shortcut layer on `PlayerClient`: `Space` toggles pause while stretching / starts while ready or rest; `ArrowRight` advances (skip → next stretch or rest → stretch); `ArrowLeft` goes back (no-op on the first stretch); `Escape` exits to `/home`. All shortcuts are neutralised by Cmd / Ctrl / Alt and by focus inside an input / textarea / contenteditable element. `SkipBack` previous button mirrors the skip-forward control (disabled on stretch 1) so arrow-key behaviour and the on-screen transport controls agree. `StretchCompletionBurst` (`src/app/player/_components/stretch-completion-burst.tsx`) is a 500 ms `framer-motion` overlay (pulsing ring + scaling check icon) that renders inside `<AnimatePresence>` when `phase === "stretching" && timeLeft <= 0`; both the burst and the player's post-zero advance branch are gated on `useReducedMotion()` — reduced-motion users see no animation and advance with a `0 ms` delay. Mobile layout pass: `min-h-screen` → `min-h-dvh` everywhere, `pt-[calc(3rem+env(safe-area-inset-top))]` + matching bottom inset on every phase container, `-m-2 p-2` on every icon-only exit control so the tap target is ≥44 px while the icon stays 20 px, and `hidden sm:block` keyboard-hint subtitle (`Space pause · ← prev · → next · Esc exit`) that never shows on mobile. `data-testid` added to every interactive element and phase container for Phase-14 Playwright (`player-ready`, `player-stretching`, `player-rest`, `player-complete`, `player-timer`, `player-pause-toggle` with `data-paused`, `player-previous`, `player-skip`, `player-start`, `player-continue`, `player-exit`, `player-complete-home`, `player-complete-library`, `player-shortcuts-hint`, `player-completion-burst`). Camera client (`src/app/player/camera/_components/camera-pose-client.tsx`) gains `<TrackingPill active={isTracking} />` over the mode toggle — green pulsing dot when MediaPipe has delivered a fresh landmark in the last 1.5 s, muted "Searching" dot otherwise (`data-testid="camera-tracking-pill"`, `data-tracking="true|false"`); driven by a 300 ms poll on `lastPoseAtRef` so a single missed frame can't flip the UI. All six camera phase overlays (`idle / loading / unsupported / no_camera / denied / error`) rewritten with `data-testid`-tagged wrappers, amber accent colour on every warning state, and two-paragraph copy (what went wrong / what to try next). Fixed-overlay root now consumes all four `env(safe-area-inset-*)` values so notched devices in landscape work. VRM bone-driver velocity-clamp smoothing: new pure helper `src/lib/pose/smoothing.ts` with `DEFAULT_MAX_ANGULAR_VELOCITY = 8 rad/s` and `velocityClampedSlerpT(angularDistance, baseAlpha, maxAngularVelocity, dt)` returning `min(baseAlpha, maxStep / distance, 1)` with defensive `0` returns for non-finite / non-positive inputs. `src/lib/pose/vrm-driver.ts::applyBone` computes `distance = bone.quaternion.angleTo(targetQuat)` and runs the slerp `t` through the clamp — MediaPipe landmark blips now move any joint by at most `8 × dt rad` per frame (~0.26 rad at 30 Hz) regardless of target-jump size, so single-frame jitter can't produce visible snaps; `applyToVrm(vrm, rig, dt)` takes R3F's `useFrame` delta (seconds) with a `1/30` fallback for legacy callers, and `avatar-view.tsx` threads `delta` through. 15 new unit tests in `tests/unit/player/keyboard.test.ts` (modifier guards, typing guard, phase guards, space / arrow / escape mapping, first-stretch back-arrow no-op) + 12 in `tests/unit/pose/smoothing.test.ts` (zero / NaN / Infinity guards, base-alpha-wins, velocity-clamp-wins, linear scaling with dt, cap at 1 for no overshoot). BDD scaffolds: `tests/features/player/player.feature` (19 scenarios covering ready → stretching → rest → complete, pause / resume, skip, previous, escape, modifier-ignored, typing-ignored, burst overlay, reduced-motion path, completion screen, mobile safe-area and hint-visibility contracts); step bindings deferred to Phase 14. No schema changes, no new external deps (framer-motion already in the tree since Phase 1), no new feature flags. 259 tests across 21 files passing.
- **Phase 9 — Billing (Stripe):** End-to-end Stripe subscription billing via hosted Checkout + signed webhook reconciliation. `src/services/billing.ts` is the ONLY file in the repo that `import`s `stripe` (parallel to the `next-auth`-only-in-`src/lib/auth.ts` rule from ADR-0004); lazy client init guarded by `STRIPE_SECRET_KEY` so `pnpm dev` / `pnpm test` never crash at boot when billing is unconfigured. `src/config/billing.ts` holds the server-side price allowlist (`getBillingPlans()` + `resolvePlanByPriceId()`) — `createCheckoutSession()` throws `UNKNOWN_PRICE` when a caller passes a priceId we didn't configure, closing the client-spoofing attack surface. New Drizzle table `stripe_webhook_events` (migration `0002_lively_arachne.sql`, columns `event_id PK / type / received_at / processed_at / payload jsonb`) is the idempotency ledger: `.onConflictDoNothing({ target: eventId })` = duplicate delivery short-circuits with no state mutation. `POST /api/billing/checkout` (`src/app/api/billing/checkout/route.ts`) gates on `auth()`, validates `CreateCheckoutSessionBodySchema`, and sources `userId` + `email` from the session (NEVER from the body — matches the Phase-3 server-sourced `userId` invariant); default success/cancel URLs derived from `NEXT_PUBLIC_APP_URL` when omitted. `POST /api/webhooks/stripe` (`src/app/api/webhooks/stripe/route.ts`) pins `runtime = "nodejs"` + `dynamic = "force-dynamic"` and reads `request.text()` (never `.json()`) so the raw body survives for HMAC verification — signature failure → 400, handler exception → 500 (Stripe retries for up to 3 days). Subscription state machine wired end-to-end: `customer.subscription.created|updated` → `mapStripeStatus(sub.status)` + persist `stripeCustomerId`; `customer.subscription.deleted` → `canceled`; `invoice.payment_failed` → `past_due`; unknown event types recorded in the ledger with no side effect. Premium-routine gate in `GET /api/routines` — resolves `viewerIsPremium = isPremium(userId)` and passes `premiumUnlocked` into `getRoutines()`; free and signed-out viewers never see `isPremium=true` rows (chose "hide entirely" over "paywall-visible" for MVP; Phase 10 can flip to "decorate" without schema changes). Zod schemas `CreateCheckoutSessionBodySchema` + `CreateCheckoutSessionResponseSchema` in `src/types/user.ts`; env validator extended with optional `STRIPE_PREMIUM_ANNUAL_PRICE_ID`. 27 unit assertions in `tests/unit/billing.test.ts` (`vi.mock("stripe")` with a constructor shim so `new Stripe(...)` works without real HTTP) cover checkout / webhook signature verification / `handleStripeEvent` idempotency / `cancelSubscription`. 7 integration scenarios in `tests/integration/api/billing-checkout.test.ts` (401 / 400 / 400 UNKNOWN_PRICE / 503 / 201 happy / default URLs / userId-email spoofing ignored) + 5 in `tests/integration/api/billing-webhook.test.ts` (400 bad sig / raw-body forwarded / 200 happy / 200 duplicate=true / 500 on handler exception); `tests/integration/api/routines.test.ts` updated to mock `@/lib/auth` + `@/services/billing` since the route now resolves both. BDD scaffolds: `tests/features/billing/checkout.feature` (6), `webhook.feature` (8), `premium-gate.feature` (6); step bindings deferred to Phase 14. OpenAPI `billing` tag + `createCheckoutSession` (201 / 400 / 401 / 503) + `stripeWebhook` (200 with `{ received, duplicate, type, eventId }` / 400 / 500). ADR-0005 locks in the 6 billing invariants (single-SDK-importer, session-sourced `userId`, priceId allowlist, raw-body HMAC, ledger-every-delivery, single `stripeCustomerId` per user) and documents four rejected alternatives (Payment Links, Elements, Paddle/MoR, polling). `DECISIONS.md` gains #15 (Stripe SDK boundary) and #16 (premium gate is a catalog filter, not a paywall). 232 tests across 19 files passing; services at 92.34% lines (billing.ts at 85.33%), global at 77.75% — all thresholds green.

### Changed
- Python hooks (`contract-guard.py`, `tdd-guard.py`, `pre-pr-gate.py`, `schema-changed.py`, `post-migration.py`) retargeted from `services/api`, `services/orchestrator`, `packages/*` layout to bendro's `src/app/api/**/route.ts`, `src/services/**`, `src/db/**`, Drizzle + pnpm conventions.
- **`userId` is now server-sourced** on `POST /api/sessions`, `PATCH /api/sessions/[id]`, and `GET /api/progress`. The server reads it from `auth()` and ignores any value in the request body or query string. `StartSession` OpenAPI schema no longer accepts `userId`; `/progress` no longer accepts a `userId` query parameter.
- `(app)` layout is now an async RSC that gates on `await auth()`.
- `src/types/next-auth.d.ts` module augmentation adds `session.user.id: string` to the Auth.js types.
- `.env.example` now shows both local-compose and Neon `DATABASE_URL` examples and points to `docs/DB_TOGGLE.md` for the workflow.

### Removed
- Creator-OS-only agents: `orchestration-lead`, `policy-lead`, `data-lead`.
- Creator-OS-only skills: `langgraph-review`, `policy-check`, `cost-tracking-check`, `workflow-adapter-check`, `evaluation-run`.
- `.claude/rules/LEGAL_RULES.md` (replaced by `HEALTH_RULES.md` — exercise/medical domain).

### Fixed
- `/api/routines?isPremium=false` was returning premium routines because `z.coerce.boolean()` coerces the string `"false"` to `true`. Replaced with `z.enum(["true","false"]).transform(…)` (Phase 2).

### Security
- Documented camera/pose privacy invariant in `SECURITY_RULES.md` and `HEALTH_RULES.md`: pose data never leaves the client.
- Added Stripe-only-in-`src/services/billing.ts` and MediaPipe-only-in-`src/lib/pose/*` architecture boundary checks to `pre-pr-gate.py`.
- **Phase 3 auth gates:** `POST /api/sessions`, `PATCH /api/sessions/[id]`, and `GET /api/progress` now require a valid Auth.js session (`UNAUTHENTICATED` otherwise). `userId` is extracted from the session server-side and overrides any client-supplied value.
- **Cross-tenant access returns `404 NOT_FOUND`, not `403`**, on `PATCH /api/sessions/[id]` to prevent session-id enumeration.
- `AUTH_SECRET` added to the env contract as `devOptional`; required in staging/production. Local dev runs without it and shows an "Auth not configured" banner on the sign-in page.

---

## Prior History (pre-framework port)

### 2026-04-17 and earlier
Scaffolded at https://github.com/dhanujdev/bendro — initial Next.js app, Drizzle schema, 6 API routes, camera/VRM integration, mock↔DB adapter. See `git log` for granular history.
